{
  "version": 2,
  "builds": [
    { "src": "fe.json", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "fe.json" }
  ]
}
