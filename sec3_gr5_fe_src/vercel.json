{
  "version": 2,
  "builds": [
    { "src": "src/fe.json", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "src/fe.json" }
  ]
}
